<% if @event.errors.any? %>
      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
<% end %>


	<div class="form-group">
 	   	<%= f.label :name, "Event Name" %>
    	<%= f.text_field :name, :class => "form-control" %>
    </div>
	
	<div class="form-group">
    	
    	<%= f.label :content, "Content" %></li>

    	<%= f.text_area :content, :class => "form-control"  %></li>
    	
	</div>